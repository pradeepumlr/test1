trigger:
- master  # or main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: UseNode@1
  inputs:
    version: '18.x'

- script: |
    npm install
    npm run build
    npm test
  displayName: 'Install, Build and Test'
